# Hinweise für Claude

- Wenn eine Datei erzeugt oder geändert wird, die zum Ausführen/Kopieren
  durch die Nutzerin/den Nutzer gedacht ist (z.B. SQL-Skripte wie
  `supabase/import_*.sql`), diese Datei am Ende der Antwort immer direkt per
  SendUserFile mitschicken – nicht nur den Dateipfad nennen.
